============
Installation
============

At the command line::

    $ easy_install django-trustpaylib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-trustpaylib
    $ pip install django-trustpaylib